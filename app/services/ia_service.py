from ..models import TouristPoint

def generate_route(user_id):
    # Simulação de roteirização (IA + Langchain)
    # Implementar a lógica real usando Langchain, baseando-se no orçamento e localização
    
    # Exemplo de retorno simulado
    route_data = {
        'points': '[{"name": "Point A", "cost": 50}, {"name": "Point B", "cost": 30}]',
        'total_cost': 80
    }
    
    return route_data
